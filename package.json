{
  "name": "snake-game",
  "version": "1.0.0",
  "description": "A simple Snake game for web deployment",
  "main": "index.html",
  "scripts": {
    "start": "npx http-server . -p 3000",
    "build": "echo 'No build step required for static files'"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  }
}